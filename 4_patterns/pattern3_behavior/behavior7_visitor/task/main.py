# Клиентский код
def main():
    pass
    # # Создаем структуру файлов
    # files = [
    #     TextFile("document.txt", 50),
    #     ImageFile("picture.jpg", 1500),
    #     VideoFile("movie.mp4", 700)
    # ]
    #
    # # Создаем посетителя для отображения информации
    # info_visitor = InfoDisplayVisitor()
    # print("Displaying file information:")
    # for file in files:
    #     file.accept(info_visitor)
    #
    # # Создаем посетителя для подсчета общего размера
    # size_visitor = SizeCalculatorVisitor()
    # for file in files:
    #     file.accept(size_visitor)
    # print(f"\nTotal file size: {size_visitor.get_total_size()} KB")


if __name__ == "__main__":
    main()
